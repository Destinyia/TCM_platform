# 中医多模态数据平台开发计划

更新时间：2026-05-20

## 1. 当前约定

本仓库以当前实现为准，后续开发必须遵守以下约定：

1. 每次修改代码、接口或数据结构后，同步更新相关文档。
2. 文档更新与代码修改必须在同一个 commit 中提交。
3. 涉及前端路由、API 路由、数据库表、接口响应字段、展示聚合规则时，优先更新 `docs/api.md`、`docs/多模态数据展示平台总体设计.md` 和本文。
4. 提交前确认只纳入本次相关文件，不提交本地 dump、临时文件、原始数据和环境目录。

## 2. 当前技术栈

| 层级 | 当前实现 | 说明 |
| --- | --- | --- |
| 后端 | Flask | `backend.app.create_app()` 注册 API 蓝图 |
| API 前缀 | `/api/demo`、`/api/ingest`、`/api/health` | 一阶段 demo 以 `/api/demo` 为主 |
| 数据库 | PostgreSQL + SQLAlchemy | 不采用 MongoDB |
| 文件资产 | 本地 `storage/` 根目录 | 生产环境可配置到 Ubuntu 本地盘或对象存储 |
| 前端 | Vue 3 + Element Plus + ECharts + Vite | 当前代码在 `frontend/src` |
| 开发环境 | Windows 仓库 + WSL 运行前端/生产近似环境 | 生产目标为 Ubuntu |

## 3. 当前核心数据结构

当前 ORM 定义位于 `backend/app/models.py`。

| 表 | 模型 | 用途 |
| --- | --- | --- |
| `dim_user` | `User` | 标准用户/患者主表，患者详情路由使用 `user_id` |
| `dim_user_identity_map` | `UserIdentityMap` | 来源平台用户身份映射 |
| `dim_name_alias_rule` | `NameAliasRule` | 姓名别名和规则版本 |
| `fact_visit` | `Visit` | 一次标准 visit，包含来源、日期、早中晚时段、质量状态 |
| `fact_modality_record` | `ModalityRecord` | visit 下的问诊、脉诊、舌诊、面诊、声诊、报告等模态记录 |
| `fact_file_asset` | `FileAsset` | 图片、PDF、音频、导出物等文件资产元数据 |
| `fact_quality_event` | `QualityEvent` | 质量事件、异常、规则证据 |
| `mart_user_day_panel` | `UserDayPanel` | 用户-日期-时段展示/分析宽表 |
| `dataset_version` | `DatasetVersion` | 数据集版本 demo 元数据 |

关键字段约定：

- `User.user_id` 是前端患者详情页主键。
- `Visit.visit_slot` 取值为 `早`、`中`、`晚`。
- `Visit.quality_status` 当前常见值为 `valid`、`incomplete`、`suspicious`。
- `ModalityRecord.modality_type` 当前包括 `ask`、`pulse`、`tongue`、`face`、`voice`、`report`。
- 文件本体不写入数据库，数据库只保存 `storage_uri`、路径、hash、大小、mime type 等元数据。

## 4. 当前 API 与前端路由索引

完整接口字段见 `docs/api.md`。修改接口时必须同步更新该文件。

### 4.1 Flask API

| API | 后端位置 | 前端调用 | 用途 |
| --- | --- | --- | --- |
| `GET /api/health` | `backend/app/__init__.py` | - | 健康检查 |
| `GET /api/demo/summary` | `backend/app/demo_api.py` | `api.summary()` | 首页概览 |
| `GET /api/demo/checkin-matrix` | `backend/app/demo_api.py` | `api.checkinMatrix()` | 打卡矩阵 |
| `GET /api/demo/users` | `backend/app/demo_api.py` | `api.users()` | 用户列表 |
| `GET /api/demo/users/{user_id}/timeline` | `backend/app/demo_api.py` | `api.userTimeline()` | 患者随访详情 |
| `GET /api/demo/visits` | `backend/app/demo_api.py` | `api.visits()` | visit 列表 |
| `GET /api/demo/visits/{visit_id}` | `backend/app/demo_api.py` | `api.visitDetail()` | visit 详情抽屉 |
| `GET /api/demo/assets` | `backend/app/demo_api.py` | `api.assets()` | 资产列表 |
| `GET /api/demo/assets/{asset_id}/file` | `backend/app/demo_api.py` | `apiUrl()` | 文件预览/下载 |
| `GET /api/demo/quality-events` | `backend/app/demo_api.py` | `api.qualityEvents()` | 质量事件 |
| `GET /api/demo/dataset-versions` | `backend/app/demo_api.py` | `api.datasetVersions()` | 数据集版本 |
| `GET /api/demo/pulse/records` | `backend/app/demo_api.py` | `api.pulseRecords()` | 脉诊记录明细 |
| `GET /api/demo/pulse/user-trend` | `backend/app/demo_api.py` | `api.pulseUserTrend()` | 单用户趋势 |
| `GET /api/demo/pulse/slot-stability` | `backend/app/demo_api.py` | `api.pulseSlotStability()` | 同槽位稳定性 |
| `GET /api/demo/pulse/cross-user` | `backend/app/demo_api.py` | `api.pulseCrossUser()` | 多用户横向对比 |
| `GET /api/demo/pulse/feature-drift` | `backend/app/demo_api.py` | `api.pulseFeatureDrift()` | 不同时段特征变化 |
| `POST /api/ingest/standard-storage` | `backend/app/ingest_api.py` | - | 标准存储入口 |
| `POST /api/ingest/parse-structured-data` | `backend/app/ingest_api.py` | - | 结构化解析入口 |

### 4.2 前端路由

前端路由定义位于 `frontend/src/router/index.js`，API 封装位于 `frontend/src/services/api.js`。

| 路由 | 组件 | 用途 |
| --- | --- | --- |
| `/` | `Dashboard.vue` | 数据概览 |
| `/checkin-matrix` | `CheckinMatrix.vue` | 打卡矩阵 |
| `/patients` | `Patients.vue` | 用户纵向随访列表 |
| `/patients/:id` | `PatientDetail.vue` | 患者随访详情 |
| `/examinations` | `Examinations.vue` | 检查/visit 总览 |
| `/pulse-analysis` | `PulseAnalysis.vue` | 脉诊下游分析 |
| `/import` | `Import.vue` | 导入说明/入口 |
| `/export` | `Export.vue` | 导出/数据集配置 |

## 5. 打卡矩阵当前规则

当前实现位于 `frontend/src/views/CheckinMatrix.vue`，后端接口为 `GET /api/demo/checkin-matrix`。

后端仍返回用户、日期、早中晚时段三层结构：

```text
rows[].cells[visit_date][visit_slot] = {
  count,
  valid_count,
  sources,
  quality_statuses,
  modalities,
  visit_ids,
  visit_times,
  status
}
```

前端展示规则：

1. 展示维度为 `用户 × 日期`，不再把早中晚拆成三个展示列。
2. 同一用户同一天的 `早`、`中`、`晚` 单元格在前端聚合。
3. 单元格只展示 `数字`、`有效/无效`、`来源`。
4. 聚合后 `count = 当天早中晚记录数之和`。
5. 聚合后 `valid_count = 当天早中晚有效记录数之和`。
6. 当 `count == valid_count` 时显示 `有效`，否则显示 `无效`。
7. 空单元格显示 `0 / 无效 / -`。
8. 缩略图和展开视图使用相同网格布局，只调整单元格密度。
9. 姓名区域可点击，跳转到 `/patients/{user_id}`。

## 6. 一阶段目标

一阶段不做自动化管线，采用本地已有静态 mock 数据构造 PostgreSQL demo 数据库，前端通过 Flask API 动态读取。

当前重点：

1. 完善多模态数据呈现形式。
2. 固化用户、visit、模态、资产、质量、数据集版本的最小结构。
3. 打通患者纵向随访、visit 多模态详情、打卡矩阵、脉诊分析的核心接口。
4. 用脉诊模块验证横向、纵向、同槽位稳定性、不同时段变化等下游分析需求。
5. 形成后续正式 ETL 可复用的数据结构和接口形态。

## 7. 本地运行流程

后端和数据库：

```bash
docker compose up -d postgres
pip install -r requirements.txt
python -m backend.scripts.init_db
python -m backend.scripts.seed_demo_data
python -m backend.run
```

前端建议在有 Node.js 的 WSL 环境运行：

```bash
cd /mnt/e/workspace/TCM_platform/frontend
npm install
VITE_API_BASE=http://localhost:5000 npm run dev -- --host 0.0.0.0
```

生产环境目标为 Ubuntu。后端也建议在 WSL/Ubuntu 语义环境下验证，减少 Windows 与 Linux 在路径、权限、依赖上的差异。

## 8. 后续里程碑

### M1：Demo 展示闭环

- 静态 mock 数据入库 PostgreSQL。
- Flask demo API 可稳定返回页面所需结构。
- Dashboard、患者列表、患者详情、打卡矩阵、检查列表、脉诊分析、导入、导出页面可用。
- 文档同步记录当前数据结构、API 和路由。

### M2：离线整理与标准化输入

- 扫描 `storage/raw/` 生成文件清单、hash、大小、mime type 和相对路径。
- 生成 `user/visit/modality/asset/quality_event` JSONL 或 CSV 中间产物。
- 固化中科、玉生堂字段映射规则和规则版本。
- 生成 mart 预览数据，验证打卡矩阵、缺失模态、重复打卡和质量状态。

### M3：正式入库与质量治理

- 批次管理、解析日志、质量事件可追溯。
- 用户身份映射支持人工审核。
- visit、modality、asset、quality_event 可重算。
- 补充 Alembic 迁移。

### M4：数据集、标签与 benchmark

- 数据集定义、版本构建、manifest 导出。
- 标签体系、标签版本、审核状态。
- benchmark 定义、run 记录、指标排行。

### M5：生产部署

- Ubuntu + Nginx + Flask + PostgreSQL 部署。
- 文件下载鉴权和审计。
- 备份、日志、权限、监控。
